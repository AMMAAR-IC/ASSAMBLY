ASSUME DS:DATA
ASSUME CS:CODE 

DATA SEGMENT
NUM DB 05D
RES DB ?
DATA ENDS 

CODE SEGMENT
START:
    MOV AX,DATA
    MOV DS,AX
    MOV AX,0000H

    MOV AL,NUM
    MOV BL,AL
    MOV CX,AL
    DEC CX
FACT:    
    DEC BL
    MUL BL
    LOOP FACT
    MOV RES,AL
    INT 03H
CODE ENDS
END START
